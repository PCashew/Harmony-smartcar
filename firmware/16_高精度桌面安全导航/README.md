# 烧录文件

- `Hi3861_wifiiot_app_allinone.bin`：用 HiBurn 烧录到 Hi3861。
- `STM32_PrecisionMotion.hex`：用 Keil/J-Link 烧录到 STM32F103。

两个固件使用同一套 10 字节串口协议，必须配套烧录。先烧 STM32，再烧 Hi3861；首次上电务必把车轮架空测试。

当前版本还包含巡航左前/正前/右前超声波扫描、转弯后的直行零点重建、短期轮迹回放和四种动作灯光。更新后必须将本目录中的两个固件都重新烧录，Hi3861 与 STM32 固件需要配套使用。
